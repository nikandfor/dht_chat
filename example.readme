# В одном месте
./main --port 3333 --dstport 3311

# В другом месте
./main --port 3311 --dstport 3333

# а потом печатать сообщения и наблюдать кучу спама.

# Если находиться за натом (или еще чем-то?), то вершинка обнаруживается сетью (потому что сама изначально знает, куда постучать?),
# но отправить ей сообщение не получается, потому что слушать сообщения на том же порту, что и dht нельзя (кэп),
# а остальные через nat (или не нат?) не пробрасываются.
